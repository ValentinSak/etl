services:
  postgres:
    image: postgres:latest
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: airflow_src
      POSTGRES_PASSWORD: airflow_src
      POSTGRES_DB: airflow_db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  postgres_etl:
    image: postgres:latest
    container_name: postgres_etl_db
    restart: always
    environment:
      POSTGRES_USER: ${AIRFLOW_DB_USER}
      POSTGRES_PASSWORD: ${AIRFLOW_DB_PASSWORD}
      POSTGRES_DB: ${AIRFLOW_DB_NAME}
    env_file:
      - .env
    ports:
      - "5433:5432"
    volumes:
      - ./etl_db/postgres_etl_data:/var/lib/postgresql/data
      - ./etl_db/procedures:/docker-entrypoint-initdb.d
      - ./etl_db/import_files:/data
      - ./etl_db/sql_scripts:/sql_scripts
      - ./etl_db/run_on_start.sh:/run_on_start.sh
      - ./shared_data_S3_replacement:/shared

  airflow:
    image: apache/airflow:2.7.3
    container_name: airflow_src
    restart: always
    depends_on:
      - postgres
      - postgres_etl
    environment:
      _AIRFLOW_DB_MIGRATE: 'true'
      _AIRFLOW_WWW_USER_USERNAME: airflow_src
      _AIRFLOW_WWW_USER_PASSWORD: airflow_src
      DEFAULT_PASSWORD: admin
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://${AIRFLOW_DB_USER}:${AIRFLOW_DB_PASSWORD}@${AIRFLOW_DB_HOST}:${AIRFLOW_DB_PORT}/${AIRFLOW_DB_NAME}
      AIRFLOW__CORE__LOAD_EXAMPLES: "false"
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./airflow_src/dags:/opt/airflow_src/dags
      - ./airflow_src/logs:/opt/airflow_src/logs
      - ./airflow_src/plugins:/opt/airflow_src/plugins
      - ./shared_data_S3_replacement:/shared
    entrypoint: >
      /bin/bash -c "airflow db migrate &&
      airflow users create --username $AIRFLOW_USER --password $AIRFLOW_USER_PASSWORD --firstname Admin --lastname User --role Admin --email admin@example.com &&
      airflow webserver & airflow scheduler"
volumes:
  postgres_data:
  postgres_etl_data:
  airflow_dags:
  airflow_logs:
  airflow_plugins: